import styled from "styled-components";

export const WrapperDarkTheme = styled.button`
  border: transparent;
  border-color: transparent;
  width: 3.5rem;
  height: 2rem;
  border-radius: 8px;
  display: grid;
  place-items: center;
  cursor: pointer;
  .toggle {
    font-size: 1.15rem;
    color: var(--text-color);
  }
`;
